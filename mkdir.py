import os

os.makedirs("python/modules/os", exist_ok=True) # here exist_ok is used to, create if it not exists, otherwise ignores

os.makedirs("python/modules/sub-process", exist_ok=True)

os.makedirs("python/modules/requests", exist_ok=True)

os.makedirs("python/modules/json", exist_ok=True)

os.makedirs("python/modules/logging", exist_ok=True)
